function showAge(name, age){
    return `The ${name}'s age is ${age}.`
}

function showCity(name, city){
    return `${name} is from ${city}.`
}

function showHobby(name, hobby){
    return `The ${name}'s hobby is ${hobby}.`
}

export {showAge, showCity, showHobby};